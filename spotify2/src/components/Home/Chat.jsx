import React from 'react';
import styled from "styled-components";

function Chat() {

  return(
    <Container>
        <h1>Chat</h1>
    </Container>
);
}

const Container = styled.div`
`;

export default Chat;